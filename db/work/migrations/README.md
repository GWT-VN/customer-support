# ⚠️ LỊCH SỬ — KHÔNG thêm migration mới vào đây

3 file này là migration schema `work` đã áp prod (lịch sử), đã gộp vào
`supabase/migrations/20250101000000_baseline.sql`.

- **Migration MỚI (kể cả cho work) → đặt ở `supabase/migrations/`** với tiền tố `work_`.
- Xem quy ước: [`../../MIGRATIONS-CONVENTION.md`](../../MIGRATIONS-CONVENTION.md).
