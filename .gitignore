# Repo git riêng — có remote riêng (AIGWTVN/GWT-Masterdata), không commit vào đây.
# Clone riêng: git clone https://github.com/AIGWTVN/GWT-Masterdata.git
GWT-Masterdata/

# Claude Code — cấu hình cục bộ cá nhân (settings.json dùng chung thì commit được)
.claude/settings.local.json

# Secrets — không bao giờ commit
.env
.env.*
!.env.example
*.key
service_role*

# macOS / OS junk
.DS_Store
._*
.Spotlight-V100
.Trashes

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# Excel temp
~$*.xlsx

# Dữ liệu di trú sinh ra (CHỨA TÊN/SĐT KHÁCH — không bao giờ commit)
tools/migrate/out/
*.tsv
load.sql
catalog.json

# Mọi file xuất ra từ DB (CHỨA TÊN/SĐT/ĐỊA CHỈ KHÁCH hoặc số liệu kinh doanh) — không bao giờ commit
Phiếu theo dõi/data/GWT_*.xlsx
/data/GWT_*.xlsx
/GWT_*.md

# ══════════════════════════════════════════════════════════════════════════════
# NGUỒN DỮ LIỆU THÔ — TUYỆT ĐỐI KHÔNG COMMIT
# Repo này là repo CODE. Data thô chỉ nằm trên máy + iCloud, không lên GitHub.
# ══════════════════════════════════════════════════════════════════════════════

# Hợp đồng POE: hợp đồng ký, báo giá, CHI PHÍ ĐẦU VÀO (giá vốn NCC) + đầu ra,
# phiếu xuất kho, PDF/scan. Lộ ra là lộ biên lợi nhuận + giá vốn nhà cung cấp.
/Các khách lọc tổng POE/

# Lịch bảo trì + lịch kỹ thuật (tên khách, địa chỉ, lịch hẹn)
/Lịch bảo trì - Lịch kĩ thuật/

# CRM leads (tên, SĐT, kênh, lịch sử mua)
/Danh sách khách hàng từ Pancake CRM.xlsx

# File CS tự theo dõi ticket lỗi (nội dung khiếu nại khách)
/260624_Ticket lỗi.xlsx

# Bảng phí dịch vụ (số liệu kinh doanh)
/GWT - Chi phí dịch vụ.xlsx

# Excel gốc Theo Dõi Bảo Hành + export Odoo (tên/SĐT/địa chỉ khách)
/Theo Dõi Bảo Hành*.xlsx
/Hệ thống CRM/
/Phiếu theo dõi/
/Báo cáo ceo/

# User gom TOÀN BỘ data thô vào 1 thư mục "File gốc/" (2026-07-24) — chứa mọi thứ
# nhạy cảm ở trên: Excel khách, CRM, hợp đồng, báo cáo CEO. Không bao giờ commit.
/data/File gốc/

# "File md/" (2026-07-24) — bản .md của CRM Pancake (tên/SĐT khách), Báo cáo CEO,
# ticket lỗi, chi phí dịch vụ. PII nặng. Không bao giờ commit.
/data/File md/

# CodeGraph — index cục bộ, sinh lại bằng `codegraph init`. Không commit.
.codegraph/

# File theo dõi nội bộ — chứa PII khách, KHÔNG commit
docs/CHECKLIST.md

# Output đối chiếu khách (có PII) — không commit
tools/migrate/_out/

# Obsidian editor config
.obsidian/
/BACKLOG.md
# Backlog tách theo khu — cùng luật với BACKLOG.md: hàng đợi việc riêng CEO↔Claude,
# có thể lỡ tay ghi tên khách. iCloud vẫn sync, không cần git.
/backlog/

# node_modules ở GỐC repo (apps/web/.gitignore lo cái bên trong apps/web)
/node_modules/

# Claude worktrees (không commit)
.claude/worktrees/

# Supabase CLI — state cục bộ (link/branch tạm), KHÔNG commit. Migrations + config THÌ commit.
supabase/.temp/
supabase/.branches/
# File dump data prod tạm (CÓ PII) — script tự xoá, chặn commit cho chắc
supabase/.tmp-*

# ── Sau tái cấu trúc 19/08/2026 ──────────────────────────────────────────
# Mọi data thô / kết quả rà soát (CÓ PII khách) gom vào data/ — KHÔNG commit.
# Chỉ giữ data/README.md để giải thích thư mục này là gì.
/data/*
!/data/README.md

# Ghi chú làm việc cá nhân qua Superpowers (plan nháp có thể dán nguyên văn
# thông tin khách — tên, SĐT, MST, địa chỉ) — không commit, chỉ máy cục bộ.
/docs/superpowers/
